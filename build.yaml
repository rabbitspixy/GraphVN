global_options:
  dart_mappable_builder:
    options:
      discriminatorKey: subclass
      build_extensions:
        'lib/{{path}}/{{file}}.dart':
          - 'lib/{{path}}/generated/{{file}}.mapper.dart'