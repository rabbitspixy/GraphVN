global_options:
  dart_mappable_builder:
    options:
      discriminatorKey: subclass
      build_extensions:
        'lib/{{path}}/{{file}}.dart':
          - 'lib/generated/{{path}}/{{file}}.mapper.dart'